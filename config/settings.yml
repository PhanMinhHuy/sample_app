user:
  name:
    max_length: 50
  email:
    max_length: 100
    regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
  password:
    min_length: 6
  page:
    limit: 20
gravatar_size_default: 100
gravatar_size_user: 70
